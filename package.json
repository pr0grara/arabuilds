{
  "name": "arabuilds",
  "version": "1.0.0",
  "private": true,
  "description": "arabuilds — lead generation systems for local business",
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=arabuilds",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
